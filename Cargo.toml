[package]
name = "kill-zen-all"
version = "0.1.0"
edition = "2021"

[dependencies]
clipboard = "0.5"
notify = "6.1"
regex = "1.10"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
